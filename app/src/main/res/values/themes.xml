<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ИграВКальмара" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/color_1</item>
        <!-- Customize your theme here. -->
        <item name="android:navigationBarColor">@color/color_1</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="o_mr1">true</item>

        <item name="android:fontFamily">@font/squid_game</item>


    </style>

    <style name="Theme.ИграВКальмара" parent="Base.Theme.ИграВКальмара" />
</resources>